# tup-angular-springboot

Estructura base del proyecto con:

- **backend/**: Spring Boot (REST + WebSocket STOMP)
- **frontend/**: Angular (servicios HTTP + WebSocket)

## Estructura

Ver carpetas `backend/` y `frontend/`.
